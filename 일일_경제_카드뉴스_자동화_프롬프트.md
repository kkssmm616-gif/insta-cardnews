# 일일 경제 카드뉴스 자동 제작 프롬프트

매일 실행하면 "오늘의 주요 경제 뉴스 3개"를 골라 각각 별도의 인스타 카드뉴스
(6컷, 표지+본문4+마무리)를 기획부터 인포그래픽 제작(+실사 사진 2컷, 브랜드 마스코트),
퍼블리시까지 끝내는 프롬프트입니다. 하루에 최대 3개의 카드뉴스 세트(게시물)를 만듭니다.
[카드뉴스_프롬프트_템플릿.md](./카드뉴스_프롬프트_템플릿.md)의 0~5단계 구조를 세트마다
반복 적용하되, "주제 선정"과 "실제 제작"까지 한 번에 수행하도록 확장했습니다.

이 문서 자체를 매일 그대로 Claude에게 전달(또는 예약 실행)하면 됩니다.
계정 정체성(컬러/폰트/핸들)은 시리즈 통일감을 위해 매일·세트마다 고정하고,
그날마다 바뀌는 것은 "어떤 뉴스 3개를 다루는가"와 "본문 카피/사진"뿐입니다.

---

## 실행 프롬프트 (그대로 복사해서 매일 사용)

```
가장 중요한 경제 뉴스 3개를 골라서 각각 별도의 인스타 카드뉴스 세트로 만들어줘.
(하루 최대 3세트, 세트당 6컷: 표지+본문4+마무리. 3~8단계는 세트 1/2/3 각각에 대해 반복한다)

0. **날짜를 먼저 한국 시간(KST, UTC+9) 기준으로 명시적으로 구한다.**
   `TZ=Asia/Seoul date +%F` (셸 환경이 다르면 이에 준하는 방법)로 오늘 날짜를 구하고,
   이후 모든 단계에서 "오늘"은 이 값을 뜻한다. 시스템/샌드박스의 기본 타임존(UTC 등)에서
   나오는 날짜를 그대로 쓰지 않는다 — 한국 시간과 최대 9시간 차이가 나서 날짜가
   하루 밀릴 수 있음 (2026-08-31 실행분이 이 문제로 "2026-08-30 중복"으로 오판되어
   건너뛰어진 사고가 실제로 있었음).
   이 날짜로 `docs/{그날짜}-cardnews-1.html`이 이미 존재하는지 확인하고,
   이미 있다면 정말 중복 실행인지(같은 날짜) 판단한다. 없다면 정상적으로 새로 만든다.
1. WebSearch로 오늘의 경제 뉴스를 6~10개 후보로 폭넓게 조사한다.
   (국내/국제 금리, 환율, 증시, 부동산, 산업/기업 이슈, 물가 등 폭넓게 훑을 것 —
    3개를 뽑아야 하므로 1개만 고를 때보다 더 넓게 조사한다)
2. 아래 기준으로 후보 중 서로 다른 카테고리의 3개를 선정하고, 각각 선정 이유를
   1~2문장으로 밝힌다. (3개가 전부 같은 카테고리로 쏠리지 않게 — 예: 전부 금리 뉴스는 금지)
3. 선정된 3개 각각에 대해, 카드뉴스_프롬프트_템플릿.md의 0~5단계를 따라 기획 문서를 만들고,
   `design/set{N}/` (N=1,2,3) 폴더의 6컷(.dc.html) 아트보드(Main, Cut2~6) 내용을
   해당 세트의 주제로 갱신한다. (세트 폴더는 이미 만들어져 있으며 스타일 스켈레톤이
   동일하게 세팅돼 있음 — 그 위에 오늘 주제의 카피·사진만 교체한다)
4. 세트별로 4단계 가이드에 따라 숫자·비교·순위 등 오늘 주제에 맞는 데이터 포인트를
   골라 각 컷의 인포그래픽 구성 요소(비교 카드, 표, 통계 박스, 팁 리스트 등)에 채운다.
   Main·Cut2 두 컷에는 4-2단계 가이드대로 오늘 주제와 맞는 실사 사진을 1장씩 넣고,
   6컷 전체에 브랜드 마스코트(부엉이)를 넣는다.
5. 세트별로 `python design/build_gallery.py {0단계에서 구한 YYYY-MM-DD} {N} "{YYYY.MM.DD} 경제 카드뉴스 — {주제 요약}"`
   실행 → `docs/{YYYY-MM-DD}-cardnews-{N}.html` 생성 → `docs/index.html`에 오늘 항목
   3개 링크를 모두 추가
6. 세트별로 오늘 주제 내용을 바탕으로 인스타그램 게시용 캡션을 직접 작성해
   `docs/{YYYY-MM-DD}-caption-{N}.txt`로 저장한다 (아래 "캡션 작성 가이드" 참고 —
   세트마다 다른 훅/CTA/해시태그를 쓰고, 3개 세트끼리도 서로 비슷해지지 않게 한다).
7. git add/commit/push (GitHub Pages가 자동으로 새 페이지 3개를 공개).
8. 주제_히스토리.md에 오늘 다룬 3개 주제와 각각 실제 발행된 GitHub Pages URL을
   세 줄로 기록한다.
```

---

## 0단계 — 오늘의 주제 선정 (3개)

### 뉴스 후보 조사
- WebSearch 최소 3~4개 쿼리로 당일 경제 뉴스를 넓게 훑는다.
  (예: "오늘 경제 뉴스", "today economic news", 금리/환율/증시/물가/산업 키워드 조합)
- 후보는 6~10개로 압축한다 (3개를 뽑아야 하므로 예전 1개 선정 때보다 폭을 넓힌다).

### 선정 기준 (우선순위 순)
1. **대중 관련성** — 2030 일반 소비자의 실생활(지갑, 소비, 취업, 자산)과 맞닿아 있는가
2. **시의성** — 오늘/이번 주에 터진 이슈인가 (며칠 지난 뉴스 재탕 금지)
3. **시각적 스토리텔링 가능성** — 사진·아이콘·숫자로 표현하기 좋은 소재인가
4. **주제 중복 회피** — `주제_히스토리.md`에 최근 7일 내 다룬 주제와 겹치지 않는가,
   **그리고 오늘 선정한 3개끼리도 서로 다른 카테고리(금리/환율/증시/부동산/산업 등)인가**
   (3개 전부 같은 카테고리 쏠림 금지)
5. **서사 구조 적합성** — 원인→현황→영향→전망(또는 대응)의 4컷 스토리로 자연스럽게 나뉘는가

### 산출물
```
세트 1 — 선정 주제: {한 줄}
세트 1 — 선정 이유: {1~2문장}
세트 2 — 선정 주제: {한 줄}
세트 2 — 선정 이유: {1~2문장}
세트 3 — 선정 주제: {한 줄}
세트 3 — 선정 이유: {1~2문장}
후보였지만 탈락한 것들: {간단히}
```

---

## 1단계 — 기획 인풋 (세트마다 각각 작성)

```
[세트 N]
주제: {선정된 경제 이슈}
타겟: 20~30대 일반 소비자 (경제 지식 초~중급)
핵심 메시지 1개: {이 뉴스가 내 삶에 어떤 의미인지, 한 줄}
컷 수: 6 (표지 1 + 본문 4 + 마무리 1)
톤앤매너: 정보전달형 + 위트 (전문 용어는 풀어서 설명)
```

---

## 2단계 — 마스터 스타일 (시리즈 고정, 세트/날짜 무관하게 재사용)

2026-09-17에 "economy.notes" 계정을 참고해 **밝은 배경의 인포그래픽 스타일**로
전면 개편했고, 2026-09-22에 "너무 색상만 따라했고 캐릭터·사진이 빠졌다"는 피드백을
반영해 **실사 사진 2컷 + 브랜드 마스코트**를 다시 추가했다 (ho.ongs_처럼 사진도 쓰고,
economy.notes처럼 캐릭터/아이콘도 쓰는 하이브리드). 브랜드 일관성을 위해 아래 스타일은
특별한 이유가 없으면 **모든 세트에서 동일하게 유지**한다.
(이미 `design/set1~3/Main.dc.html` 등 기존 아트보드에 구현된 값 — 새 컷을 만들 때
그 CSS `<style>` 블록을 그대로 복사해서 쓴다)

```
- 비율: 1080x1350px
- 배경: 따뜻한 크림색 (#F6F1E7) + 옅은 도트 패턴
- 텍스트/포인트 컬러: 네이비 (#1A2B45, 헤드라인·번호뱃지·콜아웃 박스)
                     + 레드오렌지 (#E8543E, 강조 숫자·하이라이트)
                     + 골드 (#F2A93B, 콜아웃 박스 안 보조 강조)
- 카드/표 배경: 흰색, 그림자 대신 `box-shadow:0 6px 0 #E4DDCE` (하단 라인 그림자)
- 폰트: Noto Sans KR 전체 사용 (헤드라인 900, 본문 500~700). Black Han Sans는 더 이상 쓰지 않는다.
- 아이콘: 간단한 인라인 SVG 라인아트(꺾은선 그래프 등) 또는 이모지 사용
- 실사 사진: Main(표지)의 `.hero-photo`, Cut2(원인)의 `.photo-card` 2곳에만 사용
  (나머지 4컷은 데이터 중심으로 깔끔하게 유지 — 4-2단계 참고)
- 브랜드 마스코트: 네이비 색 부엉이 캐릭터(SVG), 6컷 전부에 말풍선과 함께 등장
  (4-3단계 참고)
- 브랜드 배지: 좌상단 흰 알약모양 배지 "● ECONOMY CARD NEWS" (네이비 테두리, 빨간 점)
- 계정 핸들: @econo.card (우측 하단 고정)
- 페이지 인디케이터: 좌측 하단 "0N / 06"
```

---

## 3단계 — 컷 구성 (세트마다 반복)

| 컷 | 역할 | 구성 요소 |
|---|---|---|
| 표지 (Main) | 후킹 | 브랜드 배지 + 태그라인, 헤드라인 2줄(`<em>`으로 핵심 단어 강조), 흰 박스 서브카피, **실사 사진(`.hero-photo`, 원형 배지로 핵심 수치 오버레이)**, 마스코트+인사 말풍선 |
| STEP 01 | 원인 | 번호뱃지(`.num-badge`)+섹션 제목, 본문 설명(`.lead`, 핵심 수치는 `<b>` 형광펜 스타일), 비교 카드 2개(`.cards`/`.card`), **실사 사진(`.photo-card`, 태그 라벨 포함)**, 마스코트+코멘트 말풍선 |
| STEP 02 | 현황/수치 | 큰 통계 박스(`.big-stat`, 네이비 배경에 흰 숫자+골드 단위), 보조 설명, 비교 표(`<table>`), 마스코트+코멘트 말풍선 |
| STEP 03 | 영향 | 본문 설명, before→after 비교 행(`.impact-row`, 취소선 이전 값 → 빨간 강조 이후 값) 1~2개, 마스코트+코멘트 말풍선 |
| STEP 04 | 전망/대응 | 번호칩이 달린 팁 리스트(`.tip-list`/`.tip`) 2~3개, 마스코트+코멘트 말풍선 |
| 마무리 | 요약+CTA | 리캡 헤드라인, 네이비 콜아웃 박스(`.callout`, 핵심 한 줄 요약), 점선 테두리 CTA 박스, 마스코트(조금 더 크게)+인사 말풍선, 하단 사인오프 문구 |

각 컷의 `<style>` 블록은 6개 파일이 거의 동일하다 — 이미 있는 `design/set{N}/` 파일을
열어 같은 클래스 이름(`.frame`, `.content`, `.topbar`, `.brand-badge`, `.main` 등)을
그대로 두고, 안의 텍스트/숫자만 오늘 주제로 바꿔 쓰는 것이 가장 빠르고 안전하다.
`.main{flex:1;display:flex;flex-direction:column;justify-content:center;}`로 본문을
감싸두면 내용이 짧은 날에도 세로 중앙에 균형 있게 배치된다.

---

## 4단계 — 데이터 시각화 구성 요소 가이드 (세트마다 반복)

사진 소싱 없이, 오늘 주제의 실제 수치(검색 결과에서 확인된 것만 — 추측 금지)를
아래 컴포넌트 중 어울리는 것에 채워 넣는다. 컴포넌트는 `design/set{N}/` 기존 파일에
이미 CSS로 구현돼 있으므로 새로 만들 필요 없이 값만 바꾸면 된다.

- **`.num-badge` + `.section-title`** — 각 STEP 컷 상단의 원형 번호(01~04)와 소제목.
  소제목은 "왜 지금 오르나"처럼 질문형이나 짧은 구문으로, 핵심 단어 1개를 `<span>`으로
  감싸 빨간색 강조.
- **`.cards` (2열 카드)** — 짧은 수치 2개를 나란히 비교할 때 (예: 현재 금리 vs 인상 확률).
  강조하고 싶은 카드에 `hot` 클래스를 추가하면 값이 빨간색으로 바뀐다.
- **`.big-stat` (큰 통계 박스)** — 오늘의 핵심 숫자 1개를 임팩트 있게 보여줄 때.
  네이비 배경에 흰색 큰 숫자 + 골드색 단위/설명.
- **`<table>`** — 3개 이하의 항목을 "이전 → 현재"로 비교할 때. 현재 값 칸에
  `class="hl"`을 주면 빨간색으로 강조된다.
- **`.impact-row` (before→after 비교)** — "내 지갑에 미치는 영향"처럼 개인 체감 수치를
  보여줄 때. 이모지 아이콘 + 라벨, 그 아래 취소선 처리된 이전 값 → 강조된 이후 값.
- **`.tip-list` (번호 팁 리스트)** — 전망/대응 컷에서 실천 항목을 2~3개 나열할 때.
- **`.callout` (네이비 콜아웃 박스)** — 마무리 컷에서 오늘의 핵심 메시지를 한 문장으로
  요약할 때. `<b>` 처리한 부분은 골드색으로 강조된다.
- **아이콘**: 필요하면 표지의 원형 SVG 그래프 아이콘(`design/set1/Main.dc.html` 참고)처럼
  간단한 인라인 SVG를 직접 그리거나, 이모지(💳✈️🏠 등)로 가볍게 대체한다.
- 모든 숫자는 **오늘 WebSearch로 확인된 실제 수치**만 쓴다. 예시/추정치는 절대 넣지 않는다.

---

## 4-2단계 — 실사 사진 소싱 규칙 (세트마다 반복, Main·Cut2 두 컷에만 적용)

- **먼저 `design/assets/library/manifest.json`을 확인한다.** 금리/환율/증시/부동산/물가/산업·기업/무역/반도체/
  고용노동/에너지원자재 등 자주 나오는 주제별로 미리 검증해둔 실사 사진 라이브러리
  (`design/assets/library/{category}/*.jpg`)가 있다. 오늘 주제와 맞는 사진이 있으면 재다운로드 없이
  그대로 재사용한다 (Main·Cut2에 서로 다른 사진 1장씩, 총 2장).
- 라이브러리에 맞는 사진이 없는 새로운 주제일 때만 새로 소싱한다:
  - Unsplash 등 **무료 상업적 이용 라이선스** 이미지만 사용 (Unsplash+/유료 이미지 `plus.unsplash.com` 금지)
  - WebSearch로 후보를 찾고, 다운로드 시 URL 파라미터로 리사이즈/압축: `?fm=jpg&q=55&w=900&auto=format&fit=crop`
  - **사용 전 반드시 Read 도구로 이미지를 열어 육안 확인**한다. 아래는 배제:
    얼굴이 알아볼 수 있게 클로즈업된 사진(초상권) / 특정 국가 화폐·정치인·종교 지도자 초상 /
    브랜드 로고·상표가 크게 노출된 사진
  - 새로 받은 사진은 `design/assets/library/{category}/`에 저장하고 `manifest.json`에도 항목을 등록해
    다음에 재사용할 수 있게 한다.
- **임베드 방법**: `.dc.html`은 완전히 자기 완결적이어야 하므로(GitHub Pages 갤러리가 파일을
  base64로 감싸 iframe에 넣는 방식) 상대 경로 `<img src="../../assets/...">`가 아니라
  반드시 data URI로 인라인 임베드한다. 큰 base64 문자열을 대화 컨텍스트에 직접 붙여넣지
  말고, 먼저 `.dc.html`에 `<img src="__PHOTO_MAIN__" alt="">`처럼 플레이스홀더를 넣어둔 뒤
  아래 스크립트로 치환한다 (200자마다 줄바꿈을 넣어 저장하므로 한 줄짜리 초거대 라인이 되지 않음):
  ```
  node design/embed-photo.js design/set{N}/Main.dc.html design/assets/library/{category}/{file}.jpg __PHOTO_MAIN__
  node design/embed-photo.js design/set{N}/Cut2.dc.html design/assets/library/{category}/{file2}.jpg __PHOTO_CUT2__
  ```
  잘못된 사진을 다시 바꿔야 할 때는 `node design/reembed-photo.js <html경로> <새이미지경로>`로
  이미 임베드된 `<img>` 하나를 통째로 교체할 수 있다 (파일에 `<img>`가 1개일 때만 사용).
- Main은 `.hero-photo`(사진 전체 배경 + 하단 그라데이션 + 우하단 흰 원형 배지에 핵심 수치),
  Cut2는 `.photo-card`(250px 높이 스트립 + 좌하단 태그 라벨)에 넣는다. 기존 CSS를 그대로 쓰면 된다.

---

## 4-3단계 — 브랜드 마스코트 사용법 (세트마다 반복, 6컷 전부)

네이비색 부엉이 캐릭터 SVG를 6컷 전부에 넣는다. 모양은 고정이고(기존 `design/set1/Cut2.dc.html` 등의
`.mascot` SVG 마크업을 그대로 복사) **말풍선 문구(`.mascot-bubble`)만 그 컷의 핵심 내용에 맞게 한 줄로
새로 쓴다** (14자 안팎, 너무 길면 `white-space:nowrap` 때문에 카드 밖으로 삐져나간다).

- Main: 인사말 (예: "오늘의 경제, 같이 볼까요?") — 매일 거의 고정 문구로 써도 된다.
- STEP 01~04: 그 컷 내용을 한 줄로 요약/코멘트하는 말풍선 (예: "지정학 리스크가 한풀 꺾였어요").
- 마무리: 인사+이모지로 마무리 (예: "내일도 같이 봐요! 👋") — 이 컷만 마스코트를 92px로 조금 크게 쓴다
  (`design/set1/Cut6.dc.html`의 날개를 위로 든 "손 흔드는" variant 참고).

새 SVG를 직접 그리지 말고 기존 파일의 `.mascot` 블록(CSS+SVG마크업)을 그대로 복사해서 쓴다 —
캐릭터 모양이 매일 달라지면 브랜드 일관성이 깨진다.

---

## 4-1단계 — 캡션 작성 가이드 (세트마다 새로 작성, 고정 템플릿 금지)

인스타그램 게시물 캡션(본문 글)을 세트별 주제에 맞춰 매번 새로 쓴다.
"오늘의 경제 카드뉴스"처럼 매일·세트마다 똑같은 문구를 반복하지 않는다.

```
docs/{YYYY-MM-DD}-caption-{N}.txt 파일에 (N=1,2,3 각각) 아래 구조로 작성:

1줄: 후킹 문장 (숫자·질문·반전 중 하나를 활용, 표지 헤드라인과 겹치지 않게 다른 각도로)
2~4줄: 오늘 이슈의 핵심을 1~2문장으로 풀어서 설명 (카드뉴스 요약이 아니라
       "왜 이게 중요한지"를 캡션만 읽어도 알 수 있게)
빈 줄
CTA 1줄: 저장/공유/댓글을 유도하는 문장 (매번 다른 표현 사용 — "저장해두세요",
        "댓글로 의견 남겨주세요", "친구에게 공유해보세요" 등 로테이션)
빈 줄
해시태그: 5~8개, 해당 세트 주제와 직접 관련된 것 위주로 구성
         (#경제뉴스 #오늘의경제 같은 고정 태그 1~2개 + 주제별 태그 3~6개를 다르게 조합)
```

**같은 날 3개 세트끼리도 훅 구조를 다르게 한다** — 예: 세트 1이 질문형 훅이면
세트 2는 숫자 대비형, 세트 3은 반전형으로. 톤앤매너는 1단계의 "정보전달형 + 위트"를
유지하되, 문장 구조·어순·이모지 사용 여부는 세트마다·매일 바꿔 기계적으로 반복되는
느낌을 피한다. 이모지는 과하지 않게 0~2개.

**주의**: 이 파일이 있으면 `.github/workflows/instagram-publish.yml`이 이 내용을
그대로 해당 세트의 인스타그램 게시물 캡션으로 사용한다. 특정 세트의 캡션 파일이
없으면 워크플로가 그 세트에 한해 자동으로 제목+고정 해시태그 캡션을 대신 만들어
게시하므로, 이 단계를 건너뛰어도 게시 자체는 실패하지 않는다 (다만 캡션이
다양해지지 않을 뿐).

---

## 5단계 — 제작·발행 파이프라인

**발행 방식: GitHub Pages + GitHub Actions로 완전 자동 (승인 불필요).**
claude.ai Artifact 발행은 매번 사람이 "허용"을 눌러야 하고 모바일 알림도 오지 않아
루틴(클라우드 자동 실행)에서는 쓸 수 없다는 게 실측으로 확인됨. 대신 이 저장소의
GitHub Pages(`https://<계정>.github.io/insta-cardnews/`)로 자동 배포하고, 실제
인스타그램 게시도 사람의 승인 없이 자동으로 진행된다 — 첫 번째 준비된 세트는
즉시, 나머지는 8시간·16시간 뒤로 자동 예약돼 순서대로 게시된다 (2026-09-16부로
승인 게이트 제거. Meta API가 이따금 게시 성공 후에도 에러를 잘못 응답하는
경우가 있어, 발행 스크립트가 에러 시 실제 게시 여부를 스스로 재확인한 뒤
판단하도록 만들어둠 — 자세한 내용은 코드 저장소의 publish 스크립트 참고).

**중요 — 루틴은 master에 직접 push하지 못하고 `claude/*` 이름의 새 브랜치에만
커밋/푸시한다** (실측 확인됨, 루틴의 "풀 리퀘스트 자동 수정" 옵션과는 무관).
그대로 두면 GitHub Pages(= master의 docs/)에 반영되지 않으므로,
`.github/workflows/auto-merge-claude-branches.yml`이 `claude/**` 브랜치가
push되면 자동으로 master에 병합하도록 되어 있다. 이 워크플로 덕분에 루틴은
평소처럼 세트별 6컷 갱신 → `build_gallery.py` → 커밋/푸시만 하면 되고, master 반영과
Pages 배포는 GitHub Actions가 알아서 처리한다.

세트 N (1, 2, 3) 각각에 대해:

1. `design/set{N}/Main.dc.html`, `Cut2~5.dc.html`(내용 컷), `Cut6.dc.html`(마무리)를
   해당 세트 주제의 오늘 카피로 갱신
2. `python design/build_gallery.py {YYYY-MM-DD} {N} "{YYYY.MM.DD} 경제 카드뉴스 — {주제 요약}"` 실행
   → `docs/{YYYY-MM-DD}-cardnews-{N}.html` 생성
   (※ `seed-canvas.mjs` 결과물은 Claude 프레임 없이 단독으로 열면 빈 화면만 뜨므로
     GitHub Pages 배포에는 쓰지 않는다. `build_gallery.py`가 각 아트보드를
     `<iframe srcdoc>`으로 격리 렌더링해 순수 정적 페이지로 만들어준다.)
3. 4-1단계 가이드에 따라 `docs/{YYYY-MM-DD}-caption-{N}.txt`를 해당 세트 주제로 새로 작성

3개 세트 작업이 모두 끝나면:

4. `docs/index.html`을 열어 오늘 항목 3개
   `<li><a href="./{YYYY-MM-DD}-cardnews-{N}.html">...</a></li>` (N=1,2,3)를 추가
5. git add/commit/push (한 번에)
   - 커밋 메시지 예: "{YYYY-MM-DD} 카드뉴스 3세트: {주제1 요약} / {주제2 요약} / {주제3 요약}"
   - 푸시 후 약 1분 내 각 `https://<계정>.github.io/insta-cardnews/{YYYY-MM-DD}-cardnews-{N}.html`에 자동 반영됨
6. (선택) 사람이 직접 확인하며 인스타 캐러셀용 이미지로 편집하고 싶을 때만,
   위 파이프라인과 별개로 `seed-canvas.mjs` + Artifact 발행을 수동으로 진행해도 됨
   (이 경우엔 매번 승인 필요 — 자동화 루틴에는 포함하지 않음)

---

## 6단계 — 발행 후 체크리스트 (세트마다 확인)

- [ ] 3개 세트가 서로 다른 뉴스/카테고리를 다루는가 (쏠림 없는가)
- [ ] 표지만 봐도 무슨 뉴스인지 짐작되는가 (세트마다)
- [ ] 숫자(통계) 출처가 실제 검색 결과에 근거하는가 (추측 수치 금지)
- [ ] 숫자·비교·표에 쓰인 값이 전부 오늘 검색 결과에 근거하는가 (추측/예시값 금지)
- [ ] Main·Cut2에 오늘 주제와 어울리는 실사 사진이 각각 들어갔는가 (민감 요소 없는가)
- [ ] 6컷 모두에 마스코트가 있고, 말풍선 문구가 그 컷 내용과 맞게 매번 새로 쓰였는가
- [ ] 컬러/폰트/핸들이 시리즈와 일관되는가
- [ ] 마지막 컷에 CTA가 포함됐는가
- [ ] `docs/{YYYY-MM-DD}-cardnews-{N}.html` 3개가 모두 생성되고 `docs/index.html`에 링크가 추가됐는가
- [ ] `docs/{YYYY-MM-DD}-caption-{N}.txt` 3개가 각 세트 주제에 맞게 새로 작성됐는가
      (세트끼리도 서로 다른 훅/CTA인지)
- [ ] git push가 완료됐는가 (풀 리퀘스트 자동 수정 옵션이 꺼져 있으면 승인 대기 상태로 멈춤)
- [ ] `주제_히스토리.md`에 오늘 항목 3개(주제 + GitHub Pages URL)가 기록됐는가

---

## 자동화 설정 (완료, 참고용)

- claude.ai/code/routines에 클라우드 루틴으로 등록되어 있으며, 매일 08:00에 자동 실행된다.
  (로컬 PC 전원 여부와 무관 — 루틴은 클라우드에서 실행됨)
- 루틴 저장소: GitHub `insta-cardnews` (공개 저장소, GitHub Pages 활성화됨)
- 루틴의 "동작(Actions)" 탭에서 "풀 리퀘스트 자동 수정"을 켜둬야 커밋/푸시가
  승인 없이 자동으로 처리된다. (꺼져 있으면 실행마다 승인 필요)
- 루틴은 master가 아니라 `claude/*` 브랜치에만 push할 수 있다. master 반영은
  `.github/workflows/auto-merge-claude-branches.yml` (GitHub Actions)이 자동으로
  병합해준다 — 사람이 매일 병합해줄 필요 없음 (2026-08-29 실행에서 문제 발견 후 추가됨).
- claude.ai Artifact 발행 액션은 루틴 내에서 항상 수동 승인이 필요하고 모바일 알림도
  오지 않으므로, 자동화 파이프라인에서는 사용하지 않는다 (5단계 참고).
- **하루 최대 3세트 → 승인 없이 8시간 간격 자동 게시**: `.github/workflows/instagram-publish.yml`이
  `docs/{date}-cardnews-1.html`, `-2.html`, `-3.html` 중 실제로 존재하는 세트만
  렌더링 → GitHub Pages 반영 확인 → 첫 세트(캐러셀+릴스)를 즉시 게시하고, 나머지는
  `instagram-publish-queue.yml`(30분 간격 cron)이 8시간·16시간 뒤 각각 자동 게시한다.
  루틴이 어떤 날 1개 또는 2개 세트만 만들어도(예: 뉴스가 부족한 날) 그만큼만
  게시되고 에러가 나지 않는다.
